Extract ONLY the JSON from {textResponse} and ensure it is a valid JSON. If you need to add brackets or quotation marks to make it valid, do so. 

Your output should contain ONLY the JSON and nothing else.